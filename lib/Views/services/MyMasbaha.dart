import 'package:flutter/material.dart';

class MyMasbaha extends StatelessWidget {
  const MyMasbaha({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("masbaha"),
    );
  }
}
