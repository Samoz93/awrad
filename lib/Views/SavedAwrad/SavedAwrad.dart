import 'package:flutter/material.dart';

class SavedAwrad extends StatelessWidget {
  const SavedAwrad({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("hi")),
    );
  }
}
